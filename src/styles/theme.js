import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    primary: '#67645E',
    secondary: 'rgba(204, 204, 204, 0.6)',
    tertiary: '#E5E5E5',
  },
});

export default theme;
